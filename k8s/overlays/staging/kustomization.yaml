apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aquashield-staging

resources:
  - namespace.yaml
  - ../../base

labels:
  - pairs:
      app.kubernetes.io/environment: staging
    includeSelectors: true
