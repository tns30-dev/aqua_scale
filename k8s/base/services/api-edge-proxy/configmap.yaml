apiVersion: v1
kind: ConfigMap
metadata:
  name: api-edge-proxy-config
data:
  nginx.conf: |
    worker_processes 1;
    pid /tmp/nginx.pid;

    events {
      worker_connections 512;
    }

    http {
      access_log /dev/stdout;
      error_log /dev/stderr warn;

      proxy_temp_path /tmp/proxy_temp;
      client_body_temp_path /tmp/client_temp;
      fastcgi_temp_path /tmp/fastcgi_temp;
      uwsgi_temp_path /tmp/uwsgi_temp;
      scgi_temp_path /tmp/scgi_temp;

      map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
      }

      map $http_x_forwarded_proto $forwarded_proto {
        default $http_x_forwarded_proto;
        '' $scheme;
      }

      map $http_origin $cors_allow_origin {
        default "";
        "https://www.aquashield.live" $http_origin;
        "https://aquashield-ms-dev-20260808.web.app" $http_origin;
      }

      upstream identity-access-service { server identity-access-service:8081; }
      upstream project-service { server project-service:8082; }
      upstream sensor-service { server sensor-service:8083; }
      upstream notification-service { server notification-service:8087; }
      upstream realtime-gateway { server realtime-gateway:8088; }
      upstream pond-service { server pond-service:8089; }
      upstream analytics-service { server analytics-service:8090; }
      upstream audit-service { server audit-service:8092; }

      server {
        listen 8080;
        server_tokens off;

        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $proxy_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        proxy_set_header X-Forwarded-Host $host;

        add_header Access-Control-Allow-Origin $cors_allow_origin always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Authorization, Content-Type, X-Requested-With, X-Correlation-Id, X-CSRFToken, X-CSRF-Token" always;
        add_header Access-Control-Allow-Credentials "true" always;
        add_header Access-Control-Expose-Headers "X-Correlation-Id" always;
        add_header Vary "Origin" always;

        if ($request_method = OPTIONS) {
          return 204;
        }

        location = /healthz {
          default_type application/json;
          return 200 '{"status":"UP"}';
        }

        location ~ ^/api/projects/[^/]+/charts(/.*)?$ { proxy_pass http://analytics-service; }
        location ~ ^/api/projects/[^/]+/feeding { proxy_pass http://pond-service; }
        location ~ ^/api/projects/[^/]+/pond-comparison { proxy_pass http://pond-service; }
        location ~ ^/api/projects/[^/]+/ponds { proxy_pass http://pond-service; }
        location ~ ^/api/projects/[^/]+/sensors { proxy_pass http://sensor-service; }

        location /api/csrf { proxy_pass http://identity-access-service; }
        location /api/auth/ { proxy_pass http://identity-access-service; }
        location /api/users { proxy_pass http://identity-access-service; }
        location /api/feature-access { proxy_pass http://identity-access-service; }
        location /api/action-controls { proxy_pass http://identity-access-service; }

        location /api/profile-types { proxy_pass http://project-service; }
        location /api/parameter-types { proxy_pass http://project-service; }
        location /api/growth-indicators { proxy_pass http://project-service; }
        location /api/projects { proxy_pass http://project-service; }

        location /api/ponds { proxy_pass http://pond-service; }
        location /api/cycles { proxy_pass http://pond-service; }
        location /api/treatments { proxy_pass http://pond-service; }
        location /api/pond-treatments { proxy_pass http://pond-service; }
        location /api/feed-types { proxy_pass http://pond-service; }

        location /api/iot-devices { proxy_pass http://sensor-service; }
        location /api/sensor-types { proxy_pass http://sensor-service; }
        location /api/project-sensors { proxy_pass http://sensor-service; }

        location /api/alerts { proxy_pass http://notification-service; }
        location /api/analytics { proxy_pass http://analytics-service; }
        location /api/audit { proxy_pass http://audit-service; }

        location /ws/token { proxy_pass http://realtime-gateway; }
        location /ws {
          proxy_pass http://realtime-gateway;
          proxy_http_version 1.1;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection $connection_upgrade;
          proxy_read_timeout 3600s;
        }

        location /api/ {
          default_type application/json;
          return 404 '{"detail":"No route (api-edge-proxy)"}';
        }
      }
    }
