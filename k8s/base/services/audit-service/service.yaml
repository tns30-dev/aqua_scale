apiVersion: v1
kind: Service
metadata:
  name: audit-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: audit-service
  ports:
    - name: http
      port: 8092
      targetPort: http
