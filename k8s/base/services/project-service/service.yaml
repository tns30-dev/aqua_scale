apiVersion: v1
kind: Service
metadata:
  name: project-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: project-service
  ports:
    - name: http
      port: 8082
      targetPort: http
    - name: grpc
      port: 9092
      targetPort: grpc
