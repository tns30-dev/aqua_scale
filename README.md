# aqua_scale

AquaShield microservices conversion — planning, tracking & session workspace.

- `cooking_tracker/main/` — decided implementation specs + master checklist
- `cooking_tracker/claude/` + `cooking_tracker/codex/` — per-agent checklists & progress trackers
- `cooking_tracker/background_context/` — grading, security, architecture context
- `CLAUDE.md` + `.claude/` — Claude Code session config (skills, agents, commands)

Implementation code lives in the separate `aquashield` repo.
