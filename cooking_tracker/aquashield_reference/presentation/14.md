# Slide 14 — Testing Artifacts (Unit · Integration · E2E · Stress)

> **📌 PPTX generation note:** This slide is a **placeholder for pre-made artefact images** (screenshots of test runs, coverage reports, k6 summaries, etc.). Leave each diagram region empty in the generated PowerPoint — the presenter will insert the screenshots manually.

**Time budget:** 1 minute.
**Audience:** MTech academic panel.
**Framing:** evidence that the CI pipeline on slide 13 actually runs — show *one screenshot per category* from a green CI run, so the panel sees real test output, real coverage numbers, and real k6 summaries instead of just stage names.

---

## Slide content (what appears on the slide)

### Title
**Test Artifacts — Proof the Pipeline Runs**

### Layout — 2×2 grid of artefact tiles

#### 🟢 Tile 1 · Unit Testing

> **[INSERT screenshot — Vitest + pytest pass summary]**
>
> Caption: *Frontend Vitest 103 / 103 pass · Backend pytest 103 / 103 pass · coverage report attached as artifact.*

#### 🧪 Tile 2 · Integration / E2E Testing

> **[INSERT screenshot — Playwright report (`frontend-playwright-report`)]**
>
> Caption: *Playwright e2e covering login → list users → onboard user → manage access flows. `page.route` mocks isolate from backend.*

#### 🛡️ Tile 3 · Security Scans (SAST + DAST + SCA)

> **[INSERT screenshot — bandit JSON summary · Semgrep SARIF · OWASP ZAP baseline report · pip-audit / npm audit output]**
>
> Caption: *Static (bandit · Semgrep), runtime (ZAP baseline), dependency (pip-audit · npm audit) — all archived as workflow artifacts per run.*

#### 🔥 Tile 4 · Load + Stress (k6)

> **[INSERT screenshot — k6 stress summary table (VU ramp · http_req_duration p95 · http_req_failed rate · checks)]**
>
> Caption: *Frontend k6 ramp 10→30→60 VUs · Backend k6 ramp 20→50→100 VUs. p95 latency and error-rate trends captured as run summary.*

---

## Speaker notes (≈ 60s — say out loud, don't put on slide)

> "Four artefact tiles, one per category from the previous slide's pipeline.

> **Top-left — Unit Testing.** Frontend Vitest and backend pytest, both at 103 passing tests on the last green run.

> **Top-right — E2E.** Playwright report covering the user-management end-to-end flows: login, user list, onboarding, access management. The traces are uploaded as a workflow artifact so the panel can replay them.

> **Bottom-left — Security.** Three reports stitched into one tile: bandit's JSON output for backend SAST, Semgrep's SARIF for frontend SAST, and the ZAP baseline HTML report for DAST. The two SCA tools — pip-audit and npm audit — also feed in here.

> **Bottom-right — Stress.** The k6 stress summary tables showing the VU ramp, p95 latency, error rate, and check pass percentage at each load step. The point of the stress run isn't to pass — it's to *find* the saturation point, which the panel can read off this table."

---

## Notes for the panel (if asked)

- *Where do the artefacts live?* — Every CI run uploads its artefacts to GitHub Actions (`Actions → run → Artifacts` panel). Retention is 7 days. Coverage HTML, Playwright traces, SARIF, ZAP reports, bandit JSON, npm audit JSON — all archived per run.
- *Are these screenshots from the latest green run?* — Yes. The presenter can demo the same screenshots live by clicking into a recent green CI run on GitHub.

---

*Last updated: 2026-05-20*
