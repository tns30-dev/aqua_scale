# IoT Devices — ERD Finalization

---

## Current State

### iot_devices
| Column | Type | Notes |
|--------|------|-------|
| iot_device_id | UUID (PK) | `gen_random_uuid()` |
| device_code | VARCHAR(64) | Unique. e.g., `'RBP-1000'` |
| device_name | VARCHAR(255) | e.g., `'IoT Device 1'` |
| status | VARCHAR(50) | `'online'`, `'offline'`, `'maintenance'` |
| config | JSONB | Default `{}` |
| is_active | BOOLEAN | Default true |
| created_at | TIMESTAMPTZ | |
| updated_at | TIMESTAMPTZ | Auto (trigger) |
| device_key | TEXT | Authentication key |

---

## Refined Schema (1 Table — no changes needed)

### iot_devices

| Column | Type | Notes |
|--------|------|-------|
| iot_device_id | UUID (PK) | |
| device_code | VARCHAR(64) | Unique. Hardware identifier. e.g., `'RBP-1000'` |
| device_name | VARCHAR(255) | Human-readable name. e.g., `'IoT Device 1'` |
| status | VARCHAR(50) | `'online'`, `'offline'`, `'maintenance'` |
| config | JSONB | Device configuration. Default `{}` |
| is_active | BOOLEAN | Default true |
| device_key | TEXT | Authentication key for device communication |
| created_at | TIMESTAMPTZ | |
| updated_at | TIMESTAMPTZ | Auto (trigger) |

> Table is clean — no columns to add or remove.

---

## Use Case Mapping

From the use case diagram (System Administrator → View IoT Device → Add/Modify IoT Device):

| Use Case | Table/Column |
|----------|-------------|
| Add/Modify code | `iot_devices.device_code` |
| Add/Modify name | `iot_devices.device_name` |
| Set Status | `iot_devices.status` |
| Set config | `iot_devices.config` (JSONB) |
| Set activate | `iot_devices.is_active` (BOOLEAN) |

---

## Relationships

```
iot_devices (1) ──→ (N) project_sensors (device assigned to project sensors)
iot_devices (1) ──→ (N) sensor_messages (raw messages received from device)
```

---

## Discussion Point for Satish

### Where does `device_key` come from?

The `device_key` column stores an authentication key (e.g., `'30fba552c320949...'`). Questions to clarify:

- Is it auto-generated by the system when a device is registered?
- Is it provided by the hardware manufacturer?
- Is it entered manually by the admin?
- Is there a key rotation/regeneration mechanism?

> To be discussed with Satish.

---

*Last updated: April 17, 2026*
