# Services Tracker — Claude

Last updated: 2026-06-05 (session 2)
Legend: ⬜ not started · 🟨 in progress · ✅ done (evidence linked) · ⛔ blocked

## Summary for Codex

- **Current focus (2026-06-05 session 2):** All 4 frontend 404 gaps CLOSED. Docker stack bug fixed (project-service missing pond+notification gRPC targets). All 9 services + gateway verified healthy end-to-end. Scope now LIMITED to application layer (services only) — Codex owns everything else.
- **All frontend 404s resolved (2026-06-05):** `/api/feature-access` + `/api/action-controls` were already implemented in identity CatalogueController — just needed nginx routes added. `/api/projects/{id}/summary/` added to project-service (SummaryService using existing pond+notification gRPC stubs: totalPonds, activeAlerts, quality score, forecast). `/api/ponds/{id}/historical/` added to pond-service (HistoricalService: delegates to ingestion GetReadings, returns time-series). All 4 verified returning correct data against the running stack.
- **PARITY CORRECTION (affects docs):** the monolith's ACTIVE TIME_ZONE is `Asia/Singapore` (config/settings/base.py — the flat settings.py 'UTC' is shadowed dead code). All reading-derived features (charts, comparison, energy) bucket/label in +08; services expose `aquashield.timezone` / `TZ_OFFSET_MINUTES` (defaults Asia/Singapore). Codex: any docs citing UTC for these paths need the same correction.
- **New shared modules (2026-06-04):** `shared-api` (gRPC protos; `identity.proto` v1 with the 5 spec RPCs) and `common` (canonical authz contract: FeatureActionEntry + AccessEvaluator + AuthzSnapshot + fail-closed AuthzSnapshotConsumer, JwtVerifier for RS256 public-key validation, EventEnvelope). Resource services consume these — semantics can't drift. NOTE: net.devh grpc starter is INCOMPATIBLE with Boot 3.4/Security 6.4 — we run plain grpc-java via a SmartLifecycle (`grpc.server.port`, `grpc.server.in-process-name`); also pinned protobuf-java 4.29.3 in parent dependencyManagement (spring-cloud-gcp BOM downgrades it → gencode/runtime clash).
- **Last completed (2026-06-04):** audit-service (Java, :8092) — append-only audit trail per main/audit_service.md. DB-ENFORCED immutability (UPDATE/DELETE blocked by trigger, IT-proven), two Pub/Sub ingress shapes (dedicated `audit.event.recorded` with strict field validation + the 15-topic business event stream derived into audit records — the bootstrap catalogue's `audit.*` subs are now consumed), idempotent on auditId, admin-only query API (events search/detail/project trail/user trail/security review). PLUS identity-access-service now PUBLISHES security audit events (login.succeeded / login.failed with reason codes + attributable actor; never credential material) — its long-PENDING audit item is closed. Previous: analytics-service (TS/Express :8090) — monolith ChartService exact-parity port with Project.GetChartConfig + Pond.ValidatePondInProject + Ingestion.GetReadings gRPC seams.
- **Blockers / questions:** None. NOTE for everyone: host ports — monolith's local postgres/redis own 5432/6379, compose stack now maps **5433 (pg) / 6380 (redis)**.

## Items

| Item | Status | Progress notes | Evidence | Updated |
|---|---|---|---|---|
| Identity and Access Service (Java) | 🟨 | Core + gRPC + AUDIT DONE: REST auth + admin mgmt, RBAC parity, Redis snapshot, refresh rotation+reuse detection, jti revocation, rate limit, 5 spec gRPC RPCs, and security audit publishing (login.succeeded/login.failed → audit.event.recorded; reason codes invalid_credentials/rate_limited; emulator-IT-asserted payload contract). 33 tests green + live smoke. PENDING: image build, k8s manifests | `docs/evidence/identity-access/` + `docs/evidence/audit-service/SHIP.md` | 2026-06-04 |
| Project Service (Java) | 🟨 | Core DONE: parity REST (profile-types/parameters/growth catalogues, projects list/detail/all with per-endpoint casing, parameter-settings, energy merge-upsert), snapshot-based authz fail-closed, Redis caches w/ invalidation, Pub/Sub events, 6 gRPC RPCs (incl. GetChartConfig). ENERGY DASHBOARD NOW REAL: full energy_dashboard.py port (hourly sums +08, kpis/trend/heatmap/summary/byPeriod/alerts/dataQuality, previous-period comparison, `{:,.2f}` strings, CPython rounding) from Ingestion GetReadings project selector — replaced the zero-stub WHOSE KEYS HAD DRIFTED (tariffPerKwh/avgKwhPerDay/avgKwhPerHour/object compareInfo are the true contract). CPython-verified oracle IT. 19 tests + cross-service smoke. PENDING: image build, k8s manifests | `docs/evidence/project-service/` + `docs/evidence/xsvc-readings/SHIP.md` | 2026-06-04 |
| Pond Service (Java) | 🟨 | Core DONE: ponds (5-state status, metadata JSONB, photo_url DDL-truth exposure), cycle lifecycle (1-based current_day oracles, display names), daily health (uq + 1..200 CHECK), stage metrics (avg→current flattening), treatments, camelCase details composition w/ stage-by-day via Project gRPC, pond comparison NOW REAL (metrics/series from Ingestion GetReadings, +08 bucketing, CPython rounding, multi-day-hourly grid fix, options sensor windows via GetReadingWindows — CPython-verified oracle IT), 5 gRPC RPCs. 17 tests. PENDING: image build, k8s manifests | `docs/evidence/pond-service/` + `docs/evidence/xsvc-readings/SHIP.md` | 2026-06-04 |
| Sensor Service (Java) | 🟨 | Core DONE: sensor-type catalogue (≥1-param rule), device registry (device_code natural key, status enum, device_key never over REST), project sensor mappings (port-required + ANY-status (device,port) uniqueness), snapshot authz, Redis device-map cache w/ invalidation, events, and the 5 gRPC RPCs incl. Ingestion's hot path (ResolveDevicePort active-only + exact unknown-device parity message). PLUS PayloadHmac in common with Python↔Java cross-language test vectors. 8 IT + 6 HMAC tests. PENDING: image build, k8s manifests | `docs/evidence/sensor-service/` | 2026-06-04 |
| Ingestion Service (Java, Pub/Sub consumer) | 🟨 | Core DONE: full parity-ordered pipeline (validate → device gate + HMAC → resolve → strict pivot → idempotent persist → events) with permanent-vs-transient ack discipline (rejected=ack+event, transient=nack→DLQ). Consumes Sensor gRPC (metadata+port) + Project gRPC (parameter catalogue, TTL-cached). Postgres demo store (Bigtable = cloud target behind ReadingStore seam). 9 ITs w/ Pub/Sub emulator + fake gRPC deps — all 9 parity oracles incl. nothing-persisted-on-hard-error. PENDING: Bigtable store impl, image build, k8s manifests | `docs/evidence/ingestion-service/` | 2026-06-04 |
| Notification Service (Java) | 🟨 | Core DONE: ThresholdService engine ported with exact parity (strict comparisons, max-wins, dedup key (pond,parameter,active), no-escalation, ack-invisible asymmetry, exact message strings w/ Python float rendering) + documented improvements (UNIQUE active-alert race guard, resolved_at, alert.resolved event). Two consumers (reading.ingested + settings invalidation), threshold cache via Project gRPC + Redis, parity REST (camelCase list + acknowledge; NO resolve endpoint — parity). 7 ITs. PENDING: notification dispatcher (email/push), pond_name enrichment (needs Pond Service), image build, k8s manifests | `docs/evidence/notification-service/` | 2026-06-04 |
| Realtime Gateway (Java WebFlux, WSS) | 🟨 | Core DONE: /ws/token mint (JWT + fail-closed snapshot → one-time Redis token), /ws first-frame AUTH w/ replay rejection + auth timeout + origin allow-list + bounded frames, ws:sub TTL + PING heartbeat, pod-local registry + Redis ws:fanout cross-pod channel, Pub/Sub bridge for reading.ingested/alert.created/alert.resolved with parity frame shapes. 5 ITs w/ REAL WebSocket client. PENDING: image build, k8s manifests, frontend wiring | `docs/evidence/realtime-gateway/` | 2026-06-04 |
| Analytics Service (TypeScript/Express) | 🟨 | Core DONE: exact chart contract (8 keys, three package shapes, verbatim error bodies + monolith validation order), engine parity (Python round2/title/Pearson, bucket label last-wins, y_parameters-empty vs lookup-failure distinction), TS auth port (jose RS256 + fail-closed snapshot), gRPC clients vs shared-api protos, metadata-only Redis cache, Dockerfile, CI lanes. TZ CORRECTED: bucketing/labels/window now Asia/Singapore (+08, `TZ_OFFSET_MINUTES`) — the earlier UTC claim came from shadowed dead settings code; oracles re-anchored. 49 vitest green. PENDING: BigQuery/Bigtable read paths (post-demo targets), k8s manifests | `docs/evidence/analytics-service/PARITY.md` (correction header) | 2026-06-04 |
| Audit Service (Java, append-only) | 🟨 | Core DONE: append-only `audit.audit_events` (UPDATE/DELETE blocked by DB trigger — IT t07), strict audit-payload consumer (required-field validation → REJECTED/ack; transient → nack→DLQ; idempotent PK on auditId) + business-envelope derivation consumers (15 catalogue subs; eventType→resource/action split, payload preserved as metadata), admin-only REST (`/api/audit/{events,events/{id},projects/{id},users/{id},security}` — 401/403/200 tested), category security|business. 7 ITs green. PENDING: per-service rich audit payloads (project/pond/sensor/notification), BigQuery/GCS cold archive, traceId wiring on GKE, image build, k8s manifests | `docs/evidence/audit-service/SHIP.md` | 2026-06-04 |
| ML placeholder (Python/FastAPI, folder+docs only) | ⬜ | — | — | — |
| LLM placeholder (Python/FastAPI, folder+docs only) | ⬜ | — | — | — |

## Log

| Date | Update |
|---|---|
| 2026-06-05 | Docker stack startup bug fixed: project-service was missing `aquashield.grpc.pond.target` + `aquashield.grpc.notification.target` in application.yml and docker-compose env — caused crash on startup; both gRPC targets now wired (pond:9094, notification:9096). All 4 frontend 404 gaps closed: nginx routes added for feature-access/action-controls → identity; SummaryService added to project-service (totalPonds/activeAlerts/quality/forecast via gRPC); HistoricalService added to pond-service (time-series via ingestion GetReadings). nginx return 404 JSON fixed (single-quote → escaped double-quote, nginx 1.27 strict). CI push trigger extended to include `dev` branch; fetch-depth: 0 added to detect-changes checkout for correct path-filter comparison. Claude scope reduced to application layer (services) — all infra/DevOps/CI/CD/data transferred to Codex. |
| 2026-06-04 | Tracker initialized. Claude `.claude` skillset rebuilt for GCP/Java/Kustomize/ArgoCD stack per `main/` specs. No service code yet. |
| 2026-06-04 | Clean repo `aquashield/` created with full monorepo layout; local compose foundation verified (postgres/redis/pubsub/bigtable). Service development unblocked — no service code yet. |
| 2026-06-04 | RESTRUCTURE (user decision): single repo `tns30-dev/aqua_scale`; `aquashield/` layer dissolved. Flat ChronoFlow-style layout — one service folder per repo root + `common/` + `shared-api/{proto,events}` + `k8s/` + `infra/` + `jmeter/`; root `pom.xml` Maven multi-module parent (Java 21, Boot 3.4.x). CI path filters/Argo paths now `<service>/**` and `k8s/overlays/...` — Codex notified to update `main/ci.md`, `main/cd.md`, `main/pub_sub_contract_docs.md` paths. |
| 2026-06-04 | identity-access-service core implemented: parity-spec-driven (agent analysis of module_user), 31 tests green (incl. Testcontainers ITs for rotation/reuse/revocation/snapshot), live smoke vs compose. Compose host ports remapped pg→5433, redis→6380 (monolith owns 5432/6379). Commit pending push. |
| 2026-06-04 | shared-api (identity.proto v1) + common (canonical authz contract, JwtVerifier, EventEnvelope) modules added; identity gRPC server live (plain grpc-java SmartLifecycle — net.devh incompatible w/ Boot 3.4). 32 tests green. protobuf-java pinned 4.29.3 (GCP BOM downgrade clash). |
| 2026-06-04 | project-service shipped: parity-spec-driven (module_project agent analysis), 17 tests + 9-step CROSS-SERVICE smoke with identity (JWT public-key verification + fail-closed snapshot authz proven). Pub/Sub events live on emulator. Found+fixed Maven scope-mediation packaging bug (test-scoped jjwt-impl stripped parser from boot jar). pubsub-bootstrap.sh idempotent + project.* topics added — Codex: please add project.created/updated/settings.updated to main/pub_sub_contract_docs.md. |
| 2026-06-04 | sensor-service shipped: parity-spec-driven (DB DDL authoritative), 8 ITs + PayloadHmac cross-language parity vectors in common (Python-signed payloads verify in Java byte-for-byte — unblocks Ingestion + Lambda bridge + simulator). ResolveDevicePort serves parameter_type_ids; Ingestion will resolve codes via Project.GetParameterCatalogue. Reactor: 63 tests green. |
| 2026-06-04 | ingestion-service shipped: parity-ordered pipeline, 9 ITs green vs Pub/Sub emulator + fake in-process Sensor/Project gRPC deps (all monolith oracles: dedup, HMAC reject, unknown device/port, strict/dup params, skew, empty-allow-list silent skip). Ack discipline implements the DLQ design. Reactor: 72 tests green. Downstream now unblocked: Notification + Realtime consume reading.ingested. |
| 2026-06-04 | notification-service shipped: ThresholdService engine ported (agent parity spec — engine lived in module_data_ingestion, alert_log only real table, dead Alert model not ported). 7 ITs green: exact messages, dedup, no-escalation, ack-rebreech asymmetry, auto-resolve, settings-event cache invalidation. UNIQUE active-alert guard closes monolith dedup race. Reactor: 79 tests green. |
| 2026-06-04 | realtime-gateway shipped: 5 ITs with a real WebSocket client (token mint fail-closed, AUTH/replay, project-scoped delivery, auth timeout, heartbeat). Demo chain complete locally. Reactor: 84 tests green. |
| 2026-06-04 | pond-service shipped: DDL-authoritative parity (photo_url exposed, phantom columns dropped, day CHECK 1..200, no stage-metric unique), comparison contract with XSVC zero-stub, 15 tests. NOTE for Codex: gateway needs a finer route — /api/projects/{id}/pond-comparison/** and /api/projects/{id}/ponds go to POND service, not Project (prefix rule alone misroutes). Reactor: 99 tests green. |
| 2026-06-04 | audit-service shipped (:8092): DB-enforced append-only trail consuming the dedicated audit topic + the full business event stream (the bootstrap catalogue's 15 `audit.*` subs are no longer aspirational). Identity now emits login.succeeded/login.failed security audit payloads (closes its PENDING; AuthFlowIT t12 asserts the payload contract incl. no-credential-material). audit-service added to CI filters+matrix AT SHIP TIME (pond lesson). 109 Java + 49 TS tests green. NOTE for Codex: audit query API is platform-admin only; gateway route `/api/audit/**` → audit service; main/pub_sub_contract_docs.md can mark the audit subscriber column as implemented. |
| 2026-06-04 | LOCAL E2E STACK shipped — ALL-DOCKER (user decision: every service in its own container for visibility + one-command onboarding): compose `app` profile runs the 9 services (aq-* containers, healthchecks, container-to-container gRPC like GKE) + nginx gateway (:8080, path-aware route table incl. the 4 REGEX exceptions the cloud gateway needs: charts→analytics, pond-comparison/ponds→pond, sensors→sensor) + one-shot pubsub-init. Entry: scripts/up.sh (dev-key gen+export → build w/ BuildKit m2/npm cache mounts → up) / down.sh / seed-demo.sh (REAL APIs: project, grant+refresh, ponds, sensor type/device/mappings, thresholds, energy settings, chart-config SQL, 145 signed telemetry msgs incl. a ph breach; local HMAC skew widened for backfill). VERIFIED on containers through the gateway: charts 8 keys w/ +08 buckets, comparison real values, energy 219.64 kWh, 1 alert, WS AUTH_OK + live sensor.reading frame through the nginx upgrade, frontend login via Vite proxy. Plain `docker compose up` stays infra-only (test suites unaffected). Gaps surfaced: feature-access/action-controls/summary/historical have no owner (frontend 404s). Runbook: docs/LOCAL_E2E.md + README quickstart. NOTE for Codex: local/gateway/nginx.conf = authoritative local rehearsal of main/api_gateway.md routing. |
| 2026-06-04 | [XSVC] CLOSED: pond comparison + energy dashboard compute from real readings. ingestion.proto extended (project_id selector, pond_id on rows, NEW GetReadingWindows min/max). common gains PyRound (CPython round-half-even on exact binary doubles — Math.round was a latent parity bug). DISCOVERY: active monolith TZ = Asia/Singapore, not UTC (settings package shadows the flat file) → analytics engine TZ-corrected too; pond/project built +08-correct. Energy zero-stub had DRIFTED KEYS — true contract restored (tariffPerKwh, avgKwhPerDay/PerHour, object compareInfo, full summary/alerts/dataQuality). Multi-day-hourly comparison grid fixed. CPython-verified oracle ITs both sides. Reactor 114 + 49 vitest = 163 green. |
| 2026-06-04 | analytics-service shipped (TS/Express :8090) + two enabling gRPC seams: `IngestionReadService.GetReadings` (ingestion :9095, time-range reads over the ReadingStore seam — Bigtable target swaps behind it) and `ProjectService.GetChartConfig` (project-service owns chart config now: V3 migration `visualisation_types`+`project_visualisations`, DDL parity, 8 catalogue names seeded exactly). Chart engine = line-cited parity port incl. Python round/title/Pearson semantics and the three package shapes; diseaseRisk/WQI stay hard [] (monolith stubs). 49 vitest + ingestion t08b + project t10 → 101 Java + 49 TS green. CI: analytics-verify/analytics-container lanes added; FIXED missing pond-service entry in the java path filters (pond CI gap since pond ship). NOTE for Codex: (1) `analytics:chart-config:{projectId}` is a new Redis key — main/redis.md catalogue has no analytics section yet; (2) gateway route `/api/projects/{id}/charts/` → ANALYTICS service (another prefix-rule exception like pond-comparison). |
