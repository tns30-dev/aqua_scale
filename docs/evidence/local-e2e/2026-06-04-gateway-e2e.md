# Local E2E evidence — 2026-06-04

Stack: compose (pg/redis/pubsub/bigtable/GATEWAY:8080) + 9 host services + seed.

## seed-demo.sh transcript (abridged)

- admin login -> 5c5cd0b9-... ; project 433fc7da-... created via POST /api/projects
- access grant + refresh (snapshot version bump honored — new token required and worked)
- ponds: Alpha 6a99d2dd-..., Beta d5bcb8fc-...
- sensor type (6 params) + device DEV-LOCAL-1 + mappings A1/A2
- thresholds ph 6.5-8.5; energy 0.25 USD, hourly>2.0
- 145 HMAC-signed telemetry messages published (72h backfill, both ponds, 1 ph breach)

## verification through the gateway (:8080)

- charts: all 8 keys; multiParameterTrends[0] = {date "2026-06-01 21:00",
  label "Jun 01 21:46", temperature 27.78, ph 7.12} (+08 hourly buckets, raw-minute label
  — engine parity visible in live data); heatmap params [do, ph, temperature, turbidity]
- pond-comparison: ammonium {pondAValue 0.25, pondBValue 0.4, percentDifference -38}
  (real readings, banker's int pct)
- energy dashboard: kpis.totalKwh 219.64 (72h x 2 ports backfill)
- alerts: 1 active (seeded ph 9.2 > max 8.5 breach -> threshold engine -> alert.created)
- realtime THROUGH nginx upgrade: POST /ws/token (bearer) -> WS /ws -> first-frame AUTH
  -> AUTH_OK -> live publish -> frame "sensor.reading" received on the open socket

## route-table notes for Codex (cloud gateway)

local/gateway/nginx.conf is the authoritative local rehearsal: the charts->analytics,
pond-comparison->pond, projects/{id}/ponds->pond and projects/{id}/sensors->sensor REGEX
exceptions are REQUIRED — plain /api/projects prefix routing misroutes all four.

## gaps surfaced (frontend calls with no owner — 404 via gateway)

/api/feature-access, /api/action-controls (identity), /api/projects/{id}/summary/,
/api/ponds/{id}/historical/ (legacy). Tracked in services tracker.
