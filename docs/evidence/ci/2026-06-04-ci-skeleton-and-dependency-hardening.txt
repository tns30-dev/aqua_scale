=== CI skeleton + dependency hardening — evidence (2026-06-04) ===

Workflows (.github/workflows/, spec: main/ci.md):
  ci.yml — path-aware monorepo pipeline:
    detect-changes (dorny/paths-filter; common/shared-api/root-pom fan out to all)
    -> java-verify matrix (JDK21 + maven cache; mvn -pl <svc> -am verify with
       Testcontainers ITs) -> per-service test-report artifacts
    -> security gates (always run): Gitleaks secret scan · Semgrep SAST (p/java +
       p/security-audit, json artifact) · Trivy fs (gate=CRITICAL exit 1, HIGH/MEDIUM
       reported in json artifact — documented ratchet plan) · CycloneDX aggregate SBOM
    -> container matrix: docker build per changed service + Trivy image scan (CRITICAL)
    -> ci-summary evidence job. permissions: contents: read; concurrency dedupe.
  perf.yml — JMeter lane SEPARATED from daily CI (spec): performance-test branch +
    workflow_dispatch w/ target URL; uploads .jtl + HTML reports; guards on plans.
  .gitleaks.toml — allowlists the published HMAC test vector + evidence transcripts.

DEPENDENCY HARDENING (the gate caught real CVEs before its first cloud run):
  Local Trivy dry-run found CRITICALs in the Boot 3.4.5 BOM stack:
    tomcat-embed-core 10.1.40: CVE-2026-41293 (improper input validation),
    CVE-2026-43512 (digest auth bypass), CVE-2026-43515 (improper authorization)
    spring-security-core 6.4.5: CVE-2025-41232 (authorization bypass)
  Fix: Spring Boot 3.4.5 -> 3.5.14 + explicit dependencyManagement pin
    tomcat-embed-* 10.1.55 (Boot 3.5.14 BOM still managed 10.1.54; property override
    only works with starter-parent, not BOM import — same class of pin as protobuf).
  Result: ZERO CRITICALs across all poms (table captured below) and ALL 84 TESTS GREEN
  after the major-version bump — the test pyramid paid for itself.

Post-hardening Trivy CRITICAL scan:
  common/identity/ingestion/notification/project/realtime/sensor/shared-api/root pom: 0

Local Semgrep dry-run (p/java + p/security-audit) on all service sources: exit 0, clean.

Deferred (tracked): deploy-handoff.yml (Artifact Registry push + Kustomize bump — needs
cloud foundation + OIDC/WIF), action SHA-pinning before cloud go-live, JMeter plans,
HIGH-severity gate ratchet, branch protection (user/GitHub-plan dependent).
