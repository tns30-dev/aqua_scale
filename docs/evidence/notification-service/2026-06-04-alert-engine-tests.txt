=== Notification Service — evidence (2026-06-04) ===

Parity finding (agent): the real alert engine is ThresholdService in module_data_ingestion
(NOT module_notification); alert_log is the only real table (the `alerts` model is dead —
no DDL, no usage — not ported). The engine moved INTO this service per the target
architecture.

Engine parity (all oracle-pinned):
  strict > / < comparisons (boundary equality = within range) · MAX wins over min ·
  severity critical/warning only · log_type mapping critical->alert, warning->warning ·
  EXACT message strings "{code} exceeded maximum: {v} > {max}" with Python float
  rendering (30.0) · dedup key (pond, parameter_code, active) — severity NOT in key
  (no escalation) · acknowledged rows invisible to dedup AND auto-resolve (re-breach
  after ack creates a NEW alert) · NULL threshold sides never breach · engine failures
  swallowed (never poison the readings pipeline).

Documented improvements over the monolith:
  PARTIAL UNIQUE active-alert index closes the query-then-insert dedup race (needed for
  concurrent Pub/Sub consumers) · auto-resolve stamps resolved_at (column existed,
  unused) · alert.resolved event (no monolith analog — clears realtime banners) ·
  consistent timestamptz (monolith mixed naive/aware).

REST parity: GET /api/alerts camelCase hand-built shape {alertId,pondId,pondName,
severity,message,timestamp,acknowledged}, active-only default, ?all/?severity/?pond
filters, snapshot project-scoping; POST /api/alerts/{id}/acknowledge — actor from JWT,
body ignored, exact response {message:"Alert acknowledged",alertId}. NOTE: monolith has
NO resolve endpoint (the UI Resolve button acknowledges) — preserved.

mvn clean verify (full reactor): 79 tests green
  common 6 · identity 32 · project 17 · sensor 8 · ingestion 9 · notification 7

NotificationIT (Pub/Sub emulator + Postgres + Redis + fake Project gRPC):
  t01 max breach -> critical alert, exact message, alert.created with broadcast-dict
      payload (current_value/threshold/log_type)
  t02 repeated breach -> dedup (row count unchanged)
  t03 boundary equality + NULL-side thresholds -> never alert
  t04 normalize -> auto-resolve (resolved only; acknowledged untouched) + alert.resolved
  t05 min breach warning (exact message) + multi-param single reading + NO escalation
  t06 REST list camelCase active-only + acknowledge semantics + ack-then-rebreech
      creates NEW alert (oracle 14) + outsider scoping
  t07 project.settings.updated -> Redis threshold cache invalidated (redis.md pair)

The alert chain is complete locally:
  reading.ingested -> threshold eval (Project gRPC thresholds, Redis-cached) ->
  alert.created/resolved + notification.requested  [Realtime Gateway = next consumer]
