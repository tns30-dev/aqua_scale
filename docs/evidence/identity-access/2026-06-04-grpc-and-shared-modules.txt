=== Identity gRPC + shared modules — evidence (2026-06-04) ===

Modules added:
- common: canonical cross-service authz contract — FeatureActionEntry, AccessEvaluator
  (single implementation of the wildcard semantics), AuthzSnapshot, AuthzSnapshotConsumer
  (fail-closed Redis read model), JwtVerifier (RS256 public-key local validation),
  EventEnvelope (Pub/Sub envelope per pub_sub_contract_docs.md)
- shared-api: gRPC proto contracts; identity.proto v1 with the 5 spec RPCs:
  ValidateToken, GetAuthorizationSnapshot, GetUserAccess, AuthorizeAction, GetProjectUsers
- identity-access-service: gRPC server implementation (plain grpc-java managed by a
  Spring SmartLifecycle; net.devh starter 3.1.0 is incompatible with Boot 3.4/Security 6.4)

Engineering notes (for the report):
- protobuf-java pinned to 4.29.3 in parent dependencyManagement — the spring-cloud-gcp
  BOM manages an older runtime (4.29.0), which violates gencode<=runtime and crashes at
  class-init. Evidence of dependency-management rigor.

mvn clean verify (full reactor: common, shared-api, identity-access-service):
  RbacServiceTest          13/13  (parity oracles)
  FeatureActionValidatorTest 6/6
  IdentityGrpcIT            1/1   (in-process gRPC end-to-end: valid/invalid token,
                                   snapshot rebuild with version+features, authorize
                                   allow/deny incl. feature + project ACL, project members)
  AuthFlowIT               11/11
  LoginRateLimitIT          1/1
  BUILD SUCCESS — 32 tests
